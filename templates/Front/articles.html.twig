{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1> Actualitées</h1>
        {% for article in articles %}
            <div class="card mt-4">
                <div class="card-header bg-redchiefs text-light">
                    {{ article.title }}
                </div>
                <div class="card-body">
                    {{ article.content }}
                    <div class="font-italic text-muted mt-2"> Créé le {{ article.createdat|date }} <br>> par [
                        ]</div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}

